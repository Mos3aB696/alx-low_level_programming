#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a:this argument take from user
 * @b:this argument take from user
 * Return: 0
*/
int add(int a, int b)
{
	return (a + b);
}

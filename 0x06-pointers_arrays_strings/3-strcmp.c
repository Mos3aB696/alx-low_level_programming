#include "main.h"
/**
 * _strcmp - function that compares two strings
 * @s1: String One
 * @s2: String One
 *
 * Return: 0
*/

int _strcmp(char *s1, char *s2)
{
	if (s1 > s2)
		return (15);
	else if (s1 < s2)
		return (-15);
	else
		return (0);
}
